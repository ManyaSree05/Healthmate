<!DOCTYPE html>
<html>
<head>
  <title>Special Health Conditions</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-image:url('https://media.istockphoto.com/id/1450240429/vector/modern-blue-medical-vector-wallpaper-futuristic-medical-icons-geometric-hexagon-shape.jpg?s=612x612&w=0&k=20&c=3kAYko8bfGlIwNrMNnEt_b9Xkj03rtbUoTk31bIPx-E=');
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin: 0;
      padding: 0;
    }

    .container {
      background-color:#def5b6;
      padding: 40px;
      border-radius: 16px;
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
      max-width: 900px;
    }

    h2, h4 {
      font-weight: 600;
      color: #343a40;
    }

    .form-control {
      border-radius: 10px;
      padding: 12px;
      font-size: 15px;
      border: 1px solid #ced4da;
      transition: all 0.3s ease-in-out;
    }

    .form-control:focus {
      border-color: #ffc107;
      box-shadow: 0 0 8px rgba(255, 193, 7, 0.4);
    }

    textarea.form-control {
      min-height: 100px;
      resize: vertical;
    }

    .btn-warning {
      background-color: #f6c23e;
      border: none;
      font-weight: 500;
      border-radius: 25px;
      padding: 10px 24px;
      font-size: 16px;
      transition: background-color 0.3s ease;
    }

    .btn-warning:hover {
      background-color: #dda20a;
    }

    .btn-link {
      text-decoration: none;
      color: #6c757d;
      font-weight: 500;
    }

    .btn-link:hover {
      color: #343a40;
      text-decoration: underline;
    }

    table {
      margin-top: 20px;
    }

    thead {
      background-color: #fff3cd;
    }

    th {
      font-weight: 600;
      text-align: center;
    }

    td, th {
      vertical-align: middle;
      text-align: center;
    }
  </style>
</head>
<body>
  <div class="container mt-4">
    <h2 class="mb-4">⚠️ Special Health Conditions</h2>

    <form action="/accounts/<%= acc.id %>/special_conditions" method="POST">
      <input type="text" name="condition1" placeholder="Condition (e.g., Diabetes)" class="form-control mb-3" />
      <textarea name="description" placeholder="Description" class="form-control mb-3"></textarea>
      <input type="date" name="diagnosed_on" class="form-control mb-3" />
      <button type="submit" class="btn btn-warning">💾 Save Condition</button>
    </form>

    <% if (history.length > 0) { %>
      <hr class="my-4" />
      <h4>📜 Health Condition History</h4>
      <table class="table table-hover table-bordered">
        <thead>
          <tr>
            <th>Condition</th>
            <th>Description</th>
            <th>Diagnosed On</th>
          </tr>
        </thead>
        <tbody>
          <% history.forEach(cond => { %>
            <tr>
              <td><%= cond.condition1 %></td>
              <td><%= cond.description %></td>
              <td><%= cond.diagnosed_on %></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    <% } %>

    <a href="/accounts/<%= acc.id %>" class="btn btn-link mt-3">← Back</a>
  </div>
</body>
</html>
