<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Upload Health Documents</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background-image:url('https://media.istockphoto.com/id/1450240429/vector/modern-blue-medical-vector-wallpaper-futuristic-medical-icons-geometric-hexagon-shape.jpg?s=612x612&w=0&k=20&c=3kAYko8bfGlIwNrMNnEt_b9Xkj03rtbUoTk31bIPx-E=');
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      margin: 0;
      padding: 0;
    }

    .card {
      background-color: #def5b6;
      border-radius: 15px;
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
      padding: 30px;
      margin: 40px auto;
      max-width: 700px;
    }

    h4, h5 {
      font-weight: 600;
      color: #3a3a3a;
    }

    .form-control {
      border-radius: 10px;
      padding: 10px;
      border: 1px solid #ced4da;
      transition: border-color 0.3s ease;
    }

    .form-control:focus {
      border-color: #4e73df;
      box-shadow: 0 0 5px rgba(78, 115, 223, 0.5);
    }

    .btn-success {
      background-color: #1cc88a;
      border: none;
      padding: 10px 20px;
      font-weight: 500;
      border-radius: 25px;
      transition: background-color 0.3s ease;
    }

    .btn-success:hover {
      background-color: #17a673;
    }

    .list-group-item {
      border: none;
      border-radius: 10px;
      margin-bottom: 8px;
      background-color: #f8f9fc;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .list-group-item a {
      font-weight: 500;
      text-decoration: none;
      color: #4e73df;
    }

    .list-group-item a:hover {
      text-decoration: underline;
    }

    .text-muted {
      font-size: 0.85rem;
    }

    .btn-link {
      text-decoration: none;
      color: #6c757d;
      font-weight: 500;
    }

    .btn-link:hover {
      text-decoration: underline;
      color: #343a40;
    }
  </style>
</head>
<body>

  <div class="card p-4 shadow">
    <h4 class="mb-3">📄 Upload Health Documents</h4>
  
    <form action="/accounts/<%= accountId %>/documents" method="POST" enctype="multipart/form-data" class="mb-4">
      <div class="mb-3">
        <input type="file" name="document" accept=".pdf" class="form-control" required />
      </div>
      <button type="submit" class="btn btn-success">Upload PDF</button>
    </form>
  
    <h5>Uploaded Files</h5>
    <% if (docs.length === 0) { %>
      <p>No documents uploaded yet.</p>
    <% } else { %>
      <ul class="list-group">
        <% docs.forEach(doc => { %>
          <li class="list-group-item">
            <a href="/uploads/<%= doc.file_name %>" target="_blank"><%= doc.file_name %></a>
            <small class="text-muted">Uploaded: <%= doc.upload_date.toDateString() %></small>
          </li>
        <% }) %>
      </ul>
    <% } %>
  
    <a href="/accounts/<%= accountId %>" class="btn btn-link mt-4">← Back to Dashboard</a>
  </div>

</body>
</html>
